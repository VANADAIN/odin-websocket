# odin-websocket

Pure Odin WebSocket client implementation (RFC 6455).

## Features

- WebSocket handshake (HTTP upgrade)
- Frame parsing and construction (text, binary, ping/pong, close)
- TLS support for `wss://` connections
- Connection state management

## Status

Early development. Part of the [odiem-ecosystem](../).
